// Delivery.js
import React from 'react';
import { View, Text, StyleSheet } from 'react-native';

const Delivery = () => {
    return (
        <View style={styles.contenido}>
          <Text>Hagamos 20 bebes</Text>
        </View>
      );
};

const styles = StyleSheet.create({
    contenido: {
      padding: 20,
    },
  });

export default Delivery;
